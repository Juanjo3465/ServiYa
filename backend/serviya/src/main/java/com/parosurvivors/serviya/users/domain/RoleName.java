package com.parosurvivors.serviya.users.domain;

/**
 * Roles del sistema. Coincide con el ENUM('CLIENT','OFFERER','ADMIN') de la tabla {@code roles}.
 */
public enum RoleName {
    CLIENT,
    OFFERER,
    ADMIN
}
