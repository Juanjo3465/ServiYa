package com.parosurvivors.serviya.admin.application.dto;

/**
 * Placeholder DTO (entrada de endpoint admin). Datos del reporte interno que crea el admin
 * al eliminar feedback inapropiado no reportado (queda con auditoría reporte + ReportAction).
 * Ver documents/project-structure/estructura-endpoints.md (POST /api/v1/admin/reviews/remove).
 * TODO: definir campos.
 */
public class CreateReportRequest {
}
